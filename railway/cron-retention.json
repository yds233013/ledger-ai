{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "ledgerai-retention-sweep",
    "cronSchedule": "0 4 * * *",
    "restartPolicyType": "NEVER"
  }
}
